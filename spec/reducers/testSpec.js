import * as matchers from 'jasmine-immutable-matchers';
import Immutable from 'immutable';

describe('Test apiKey reducer', () => {
    // beforeEach(function () {
    //     jasmine.addMatchers(matchers);
    // });
    //
    // it("should return new state", () => {
    //     expect(Immutable.Map()).toBeImmutable();
    // })
});
